# OpenClaw Hardware Scheduler Plugin

Build:

```bash
npm install
npm run build
npm pack
```

Runtime config defaults:

- `endpoint=http://127.0.0.1:8765`
- `mode=observe`
- `failOpen=true`
- `sendRawParams=false`
